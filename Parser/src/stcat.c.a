#include <stdio.h>
#include <string.h>

int main()
{
    char example[100];

    strcpy(example,"IPM.ir");
    strcat(example," is over 10");
    strcat(example, " year old.");

    printf("%s\n", example);
    return 0;

}